import React from 'react';
import './Resume.scss';

/**
 * Class created to display the file that is uploaded
 * Specifically a pdf file
 * If no file uploaded, ask to import a file
 * if file not .pdf suffix, error and show no file screen again
 */

/**
 * fred's playground
 */
class Resume extends React.Component{
    
}
export default Resume;